#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
 
int main() {
    double R,volume;
    const double pi=3.14159;
    cin >> R;
    volume = (4.0/3) * pi * pow(R,3);
    cout << fixed << setprecision(3);
    cout << "VOLUME = " << volume << endl;
   
    return 0;
}
